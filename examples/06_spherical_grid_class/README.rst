SphericalMesh
=============

Examples demonstrating the :class:`~pyvisual.core.mesh3d.SphericalMesh` class
— the primary data container for structured :math:`(r, \theta, \phi)` grids.
Topics include construction from NumPy arrays, arithmetic and NumPy ufunc
support, and the filter methods for radial scaling and coordinate remapping.