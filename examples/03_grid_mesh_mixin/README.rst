Grid Mesh Mixin
===============

Examples demonstrating :class:`~pyvisual.core.mixins.GridMeshMixin` — the
mixin that renders structured-grid data from independent 1-D axis arrays or
pre-broadcast 3-D arrays, including fieldline tracing with
:mod:`mapflpy`.
